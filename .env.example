# MySQL 镜像（按顺序尝试，哪个能 pull 用哪个）
#
# 1. Docker Desktop 内置 Hub 代理（推荐，走本地，不经过外网镜像站）
MYSQL_IMAGE=hubproxy.docker.internal:5555/library/mysql:8.0
#
# 2. 国内镜像站（若上面失败再改）
# MYSQL_IMAGE=docker.m.daocloud.io/library/mysql:8.0
# MYSQL_IMAGE=docker.1ms.run/library/mysql:8.0
#
# 3. Docker Hub 直连（需配置 registry-mirrors 或代理）
# MYSQL_IMAGE=mysql:8.0
