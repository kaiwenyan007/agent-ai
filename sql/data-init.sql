-- 可选：演示用初始数据（不要在生产环境直接执行）
-- 默认密码均为：demo123456（BCrypt，在 v0.2 注册接口实现后由程序生成）

USE agent_ai;

-- 示例用户（v0.2 完成后可改由注册接口创建，此处仅作联调参考）
-- INSERT INTO users (username, password_hash) VALUES ('demo', '$2a$10$placeholder');
